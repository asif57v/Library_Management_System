{
  "name": "library_management",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite\" \"npm run start --prefix server\"",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server/index.js",
    "postinstall": "npm install --prefix server && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vite": "^8.0.1"
  }
}
